# ✅ Фаза 2 готова к реализации!

**Дата**: 2025-10-30
**Статус**: 🟢 План готов, можно начинать

---

## 🎯 Что завершено (Фаза 1)

- ✅ **ReversibleCapsule v1 (CPU)**: полная реализация encode/decode
- ✅ **PRP-ядро (Feistel)**: 10 раундов, Blake2s
- ✅ **EnergyMapper**: конвертация bytes ↔ digits ↔ energies
- ✅ **CapsuleUI**: визуализация с метриками и snapshot
- ✅ **Инфраструктура**: ConfigCenter, LoggingHub, ProcessRegistry
- ✅ **Тесты**: полное покрытие CapsuleCoreTests

---

## 🚀 Что планируем (Фаза 2)

### § 2. EnergeticRouter Core v1 (CPU)

Реализуем полнофункциональный энергетический роутер на CPU с четырьмя основными компонентами:

#### 2.1 Структуры графа (2-3 часа)
- **GraphTypes**: NodeID, Edge, LayerConfig, GraphConfig
- **Graph**: CSR-представление (rowPtr, colIdx, weights)
- **GraphBuilder**: генератор решётки 10×1024 с локальными + jump соседями

#### 2.2 Forward-pass CPU (3-4 часа)
- **RouterParams**: Q, K, bias, инициализация Xavier
- **ForwardPassCPU**:
  - Логиты через dot(Q[j], K[k])
  - Softmax с температурой τ
  - Распределение энергии α·e·π_jk
  - Top-K отбор
  - **Accelerate/vDSP** для производительности

#### 2.3 Loss & Backprop (3-4 часа)
- **Loss**: MSELoss, CrossEntropyLoss, EntropyRegularization
- **BackwardPassCPU**: градиенты по Q, K, bias (softmax backward)
- **AdamOptimizer**: моменты m, v, bias correction
- **Численная проверка**: finite differences для валидации

#### 2.4 Logging & Metrics (2-3 часа)
- **RouterLogger**: события router.forward, router.backward
- **MetricsCollector**: actor для thread-safe сбора
- **PipelineSnapshot**: расширение полями роутера
- **MetricsExporter**: экспорт в CSV/JSON

---

## 📚 Документация

### Основные документы:

1. **`phase2_router_implementation_plan.md`** (21 KB)
   - Детальный план с файловой структурой
   - API эскизы для каждого компонента
   - Критерии завершения для каждого этапа
   - Риски и смягчение

2. **`phase2_router_summary.md`** (4.6 KB)
   - Краткая сводка с таймингом
   - Ключевые компоненты
   - Технические детали

3. **`phase2_architecture_diagram.md`** (29 KB)
   - Визуальные диаграммы архитектуры
   - Поток данных forward/backward
   - CSR-структура графа
   - Training loop
   - Численные проверки

### Контекстные документы:

- `phase2_execution_plan.md`: общий план фазы 2
- `plan_snn_router_swift_macos.md`: архитектура роутера
- `config_center_schema.md`: схема конфигов
- `baseline.yaml`: полная конфигурация

---

## 🗂️ Структура реализации

### Новые файлы в Sources/EnergeticCore/:

```
EnergeticCore/
├── GraphTypes.swift           ← § 2.1 (базовые типы)
├── Graph.swift                ← § 2.1 (CSR-граф)
├── GraphBuilder.swift         ← § 2.1 (генератор)
├── RouterParams.swift         ← § 2.2 (параметры)
├── ForwardPassCPU.swift       ← § 2.2 (forward)
├── BackwardPassCPU.swift      ← § 2.3 (backward)
├── Loss.swift                 ← § 2.3 (loss функции)
├── Optimizer.swift            ← § 2.3 (Adam)
├── Metrics.swift              ← § 2.4 (метрики)
├── RouterLogger.swift         ← § 2.4 (логирование)
└── MetricsExporter.swift      ← § 2.4 (экспорт)
```

### Новые тесты в Tests/EnergeticCoreTests/:

```
EnergeticCoreTests/
├── GraphTypesTests.swift
├── GraphTests.swift
├── GraphBuilderTests.swift
├── RouterParamsTests.swift
├── ForwardPassCPUTests.swift    ← численная проверка!
├── BackwardPassCPUTests.swift   ← finite differences!
├── LossTests.swift
├── OptimizerTests.swift
├── MetricsTests.swift
├── RouterLoggerTests.swift
└── MetricsExporterTests.swift
```

---

## 📊 Порядок реализации

Рекомендуемая последовательность для максимальной эффективности:

### 📅 День 1: Структуры данных
```
Этап 1: § 2.1 Структуры графа (2-3 часа)
  1. GraphTypes.swift + тесты
  2. Graph.swift + тесты
  3. GraphBuilder.swift + тесты
  ✅ Критерий: swift test --filter GraphTests проходит
```

### 📅 День 2: Forward-pass
```
Этап 2: § 2.2 Forward-pass CPU (3-4 часа)
  1. RouterParams.swift + тесты
  2. ForwardPassCPU.swift + тесты
  3. Metrics.swift + тесты
  ✅ Критерий: сохранение энергии, корректность softmax
```

### 📅 День 3: Backprop
```
Этап 3: § 2.3 Loss & Backprop (3-4 часа)
  1. Loss.swift + тесты
  2. BackwardPassCPU.swift + тесты
  3. Optimizer.swift + тесты
  ✅ Критерий: градиенты совпадают с численными
```

### 📅 День 4: Интеграция
```
Этап 4: § 2.4 Logging & Integration (2-3 часа)
  1. RouterLogger.swift + тесты
  2. Обновить PipelineSnapshot
  3. MetricsExporter.swift + тесты
  ✅ Критерий: логи в LoggingHub, экспорт метрик
```

**Общее время**: 10-14 часов (распределено на 3-4 дня)

---

## ✅ Критерии завершения Фазы 2

### Функциональность:
- [ ] Граф 10×1024 генерируется из `baseline.yaml`
- [ ] Forward-pass работает с Accelerate/vDSP
- [ ] Backward-pass корректен (finite differences ✓)
- [ ] Adam оптимизатор обновляет параметры
- [ ] MSE/CE loss реализованы

### Тестирование:
- [ ] `swift test --filter EnergeticCoreTests` проходит
- [ ] Численная проверка градиентов: `|num_grad - ana_grad| < 1e-4`
- [ ] Сохранение энергии: `|output - α·input| < 1e-4`
- [ ] Softmax нормализация: `|sum(π) - 1.0| < 1e-6`

### Логирование:
- [ ] События `router.forward`, `router.backward`, `router.checkpoint`
- [ ] MetricsCollector собирает метрики
- [ ] Экспорт в CSV/JSON работает
- [ ] PipelineSnapshot обновлён

### Качество:
- [ ] `swift build -c release` проходит без warnings
- [ ] Все файлы содержат документацию
- [ ] Код соответствует WARP-принципам

---

## 🔍 Ключевые технические решения

### 1. CSR-формат графа
Выбран для эффективной навигации по рёбрам:
```swift
rowPtr[j]..rowPtr[j+1]  // диапазон рёбер узла j
colIdx[i]                // целевой узел для ребра i
weights[i]               // вес ребра i
```

**Преимущества**:
- Кэш-локальность при проходе по соседям
- O(1) доступ к рёбрам узла
- Совместимость с Accelerate sparse ops

### 2. Использование Accelerate
```swift
vDSP_dotpr()   // dot products для логитов
vDSP_vsmul()   // масштабирование энергии
vDSP_maxv()    // top-K отбор
```

**Ожидаемая производительность на M4**:
- Forward pass: ~5-10 ms (10×1024)
- Backward pass: ~8-15 ms
- Total iteration: ~15-30 ms

### 3. Численная стабильность
- Softmax: log-sum-exp trick
- Температура τ ≥ 1.0
- Клиппинг энергий/весов
- NaN/Inf guards

### 4. Градиентная проверка
Обязательная численная проверка для всех параметров:
```swift
ε = 1e-5
numerical_grad = (loss(θ+ε) - loss(θ-ε)) / (2ε)
assert |numerical_grad - analytical_grad| < 1e-4
```

---

## 🎯 После Фазы 2

Следующие шаги (из `phase2_execution_plan.md`):

### § 3. Интеграция Capsule ↔ Router
- CapsuleBridge: encode → energies → router → decode
- End-to-end тест: строка → router → строка
- CLI demo команда

### § 4. Метрики и визуализация
- Обновление EnergeticUI
- GraphView для отображения маршрутов
- Top-K рёбер, энергопотоки

### § 5. Тестирование и качество
- Бенчмарки (Instruments Time Profiler)
- Профилирование энергопотребления
- Обновление документации

---

## 📌 Важные заметки

### Конфигурация из baseline.yaml:
```yaml
router:
  layers: 10
  nodes_per_layer: 1024
  neighbors:
    local: 8     # соседи в слое j+1
    jump: 2      # соседи в слое j+2
  alpha: 0.9     # коэффициент распределения
  tau: 3.0       # температура softmax
  top_k: 4       # активных рёбер
  optimizer:
    type: adam
    lr: 1.0e-3
```

### Process IDs для логирования:
```
router.forward      → forward-pass метрики
router.backward     → loss, grad_norm
router.checkpoint   → сохранение весов
```

### Зависимости:
- SharedInfrastructure ✅ (готова)
- CapsuleCore ✅ (готова)
- Accelerate (системный)
- Foundation (системный)

---

## 🚦 Статус: ГОТОВ К РЕАЛИЗАЦИИ

Все материалы подготовлены:
- ✅ Детальный план (21 KB)
- ✅ Краткая сводка (4.6 KB)
- ✅ Архитектурные диаграммы (29 KB)
- ✅ Конфигурация baseline.yaml
- ✅ Инфраструктура из фазы 1
- ✅ Документы закоммичены и запушены

**Можно начинать с § 2.1 (GraphTypes.swift)! 🚀**

---

_Инженерно-скептический подход: каждый компонент сопровождается тестами и численными проверками. Вероятность успеха: ≈ 90 %._
